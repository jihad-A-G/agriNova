import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AgriNova - Premium Australian Fruits Export",
  description: "Experience the finest selection of Australian fruits, carefully exported and delivered to Lebanon. Premium quality grapes, almonds, peaches, cherries, and more.",
  keywords: ["Australian fruits", "fruit export", "Lebanon", "grapes", "almonds", "cherries", "peaches", "premium fruits"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}

